apiVersion: apps/v1
kind: Deployment
metadata:
  name: packages-taback
  labels:
    app: packages-taback
spec:
  replicas: 1
  selector:
    matchLabels:
      app: packages-taback
  template:
    metadata:
      labels:
        app: packages-taback
    spec:
      containers:
        - name: packages-taback
          image: dirgo11/packages-taback:latest
          ports:
            - containerPort: 8003
          env:
            - name: SERVER_PORT
              value: "0"
            - name: HOSTNAME
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: BD_URL
              value: "jdbc:postgresql://postgres-service:5432/BD_TravelAgency"
            - name: BD_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: username
            - name: BD_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: password
            - name: KC_REALM_NAME
              value: "TravelAgency"
            - name: KC_HOST
              value: "keycloak-service:9090"
            - name: KC_ISSUER_HOST
              value: "localhost:9090"
            - name: KC_USER_CONSOLE
              value: "admin"
            - name: KC_USER_PASSWORD
              value: "admin"
---
apiVersion: v1
kind: Service
metadata:
  name: packages-taback
spec:
  selector:
    app: packages-taback
  ports:
    - port: 8080
      targetPort: 8003